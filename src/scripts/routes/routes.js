import List from '../views/pages/list';

const routes = {
  '/': List, // default page
  // '/now-playing': NowPlaying,
  // '/upcoming': Upcoming,
  // '/detail/:id': Detail,
  // '/like': Like,
};

export default routes;
